import next from 'eslint-config-next';
import next_core_web_vitals from 'eslint-config-next/core-web-vitals';
import prettier from 'eslint-config-prettier';

export default [next, next_core_web_vitals, prettier];
